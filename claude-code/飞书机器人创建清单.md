# 飞书 Bot 创建 Checklist

> Sources: Hands-on, verified 2026-05-12
> Updated: 2026-05-13

## Overview

创建飞书机器人桥接 Claude Code 的完整流程。如果只要改已有 bot 的名字/头像/描述，跳到[变更](#变更已有bot)。

---

## 新建 Bot（7步）

### Step 1: 创建应用
1. 打开 https://open.feishu.cn/app
2. 点「创建企业自建应用」
3. 填名称 + 描述
4. 上传头像（240×240 以上 PNG/JPG）
5. 点「创建」

### Step 2: 复制凭据
1. 左侧点「凭证与基础信息」
2. 复制 **App ID** 和 **App Secret**（点复制按钮，非重置按钮）

### Step 3: 添加机器人能力
1. 左侧点「添加应用能力」
2. 找到「机器人」→ 点「添加」

### Step 4: cc-connect 自动配置
```bash
./cc-connect feishu setup --project <项目名> --app <App ID>:<App Secret>
```

### Step 5: ⚠️ 手动配置权限（必须手动）

⚠️ **cc-connect 对非扫码创建的应用无法自动配权限，必须手动操作。**

左侧点「**权限管理**」→「**去开通权限**」→ 搜索并勾选以下 **5个权限**：

| 权限 Key | 说明 |
|----------|------|
| `im:message` | 获取与发送消息 |
| `im:message:send_as_bot` | 以机器人身份发消息 |
| `im:message.p2p_msg:readonly` | 读取单聊消息 |
| `im:chat:readonly` | 获取群信息 |
| `contact:user.base:readonly` | 获取用户基本信息 |

### Step 6: ⚠️ 手动配置事件（必须手动）

左侧点「**事件与回调**」：
1. 订阅方式 → 选「**使用长连接接收事件**」
2. 点「添加事件」→ 搜索 `im.message.receive_v1` → 勾选 → 确认

### Step 7: 发布
1. 左侧「**版本管理与发布**」→「**创建版本**」
2. 版本号默认即可 → 点「保存」
3. 弹窗点「确认发布」（免审核，自动通过）
4. 约 5 分钟后生效，重启 cc-connect

---

## 变更已有 Bot

如果只改名字/头像/描述，不需要重建：

1. 打开应用详情页 →「凭证与基础信息」
2. 点「国际化配置」→ 改名称/描述 → 保存
3. 点头像区域 → 上传新头像 → 保存
4. 「版本管理与发布」→ 创建新版本 → 发布

---

## 多项目多 Bot

一个项目一个 bot，在 `~/.cc-connect/config.toml` 中分别配置，各自独立运行。

```toml
[[projects]]
name = "project-a"

[projects.agent]
type = "claudecode"
[projects.agent.options]
work_dir = "E:/Projects/A"

[[projects.platforms]]
type = "feishu"
[projects.platforms.options]
app_id = "cli_xxx"
app_secret = "xxx"

[[projects]]
name = "project-b"
# ... 同上，用不同的 app_id/app_secret
```

---

## 常见问题

| 问题 | 原因 | 解法 |
|------|------|------|
| 聊天界面白屏 | Step 5/6 没做 | 补上权限和事件订阅，重新发布 |
| 发消息没反应 | 事件订阅未配或 cc-connect 未重启 | 检查长连接模式 + 重启 cc-connect |
| App Secret 复制不了 | 点了重置按钮 | 复制按钮在 Secret 文字右边，别点成重置 |
| cc-connect 连不上 | 应用未发布 | 创建版本并发布 |
